import React from 'react';
import Counter from './counter'; // file should exists in same src folder. imports counter component from the local file

function App() {
  return (
    <div>
      <h1>Welcome to React Class Component Demo</h1>
      <Counter />
    </div>
  );
}

export default App;
